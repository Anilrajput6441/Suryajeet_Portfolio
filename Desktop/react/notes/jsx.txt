jsx is not HTML in  javascript but html like syntax known as javascript XML
Feature	JSX (JavaScript XML)
Definition	A syntax extension of JavaScript used in React.

HTML (HyperText Markup Language)-->Standard markup language for web pages
	
Feature	JSX (JavaScript XML)-->Usage	Used in React components to render UI.

HTML (HyperText Markup Language)-->Used to structure web content.
	
Feature	JSX (JavaScript XML)-->Syntax Similar to HTML but allows embedding JavaScript expressions {}.

HTML (HyperText Markup Language)-->Standard HTML elements with attributes.
	
Feature	JSX (JavaScript XML)-->Attributes	Uses camelCase (e.g., className, onClick).

HTML (HyperText Markup Language)-->Uses lowercase (e.g., class, onclick).
	
Feature	JSX (JavaScript XML)-->Dynamic Content	Allows JavaScript expressions directly exectue

html-->	Can use <img> without /
jsx--> Requires explicit self-closing (e.g., <img />).